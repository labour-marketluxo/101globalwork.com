import { createSupabaseServerClient } from '@/lib/supabase/server';

export const metadata={title:'Discovery & SEO',robots:{index:false,follow:false}};

export default async function DiscoveryPage(){const supabase=await createSupabaseServerClient();const [{count:indexable},{count:blocked},{count:services},{data:routes}]=await Promise.all([supabase.from('public_routes').select('*',{count:'exact',head:true}).eq('indexability','indexable'),supabase.from('public_routes').select('*',{count:'exact',head:true}).neq('indexability','indexable'),supabase.from('taxonomy_entities').select('*',{count:'exact',head:true}).eq('kind','service'),supabase.from('public_routes').select('canonical_path,indexability,quality_score,updated_at').order('updated_at',{ascending:false}).limit(12)]);return <div className="admin-page"><header className="admin-page-header"><div><p className="eyebrow">Discovery & SEO</p><h1>Control what deserves to be found.</h1><p>Canonical URLs, quality gates and indexability stay explicit. Thin pages do not become search inventory by accident.</p></div></header><section className="admin-stat-grid"><article><span>Indexable routes</span><strong>{indexable??0}</strong><small>Passed quality gates</small></article><article><span>Held from indexing</span><strong>{blocked??0}</strong><small>Noindex or quality-limited</small></article><article><span>Canonical services</span><strong>{services??0}</strong><small>Immutable taxonomy identities</small></article></section><section className="admin-section"><div className="admin-section-heading"><div><h2>Recent route decisions</h2><p>Representation can change; canonical entity IDs remain authority.</p></div></div>{routes?.length?<div className="admin-list">{routes.map(route=><article key={route.canonical_path}><div><strong>{route.canonical_path}</strong><span>{route.indexability.replaceAll('_',' ')}</span></div><small>Quality {route.quality_score??'—'} · {new Date(route.updated_at).toLocaleString()}</small></article>)}</div>:<p className="empty-admin">No public routes yet.</p>}</section></div>}
