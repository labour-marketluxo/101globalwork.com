/**
 * Discovery domain types. Intended home for public discovery documents,
 * service/location taxonomy and indexability shapes (maps to
 * features/discovery).
 *
 * Note: `PublicDiscoveryDocument` currently lives in
 * lib/discovery/public-page.ts and `PublicProviderProfile` in
 * lib/providers/public-profile.ts.
 */
export {};
