/**
 * Auth domain types. Intended home for account, session, MFA and
 * administrator-activation shapes (maps to features/auth).
 *
 * Placeholder — see types/index.ts for the consolidation plan.
 */
export {};
