/**
 * Provider domain types. Intended home for provider profiles, onboarding
 * progress, search readiness, verifications and assignments (maps to
 * features/provider-workspace).
 *
 * Placeholder — see types/index.ts for the consolidation plan.
 */
export {};
