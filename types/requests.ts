/**
 * Requests domain types. Intended home for request lifecycle, quotes,
 * matching and completion-approval shapes (maps to features/requests).
 *
 * Placeholder — see types/index.ts for the consolidation plan.
 */
export {};
